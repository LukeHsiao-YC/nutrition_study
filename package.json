{
  "name": "nutrition-papers",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "astro": "^4.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
